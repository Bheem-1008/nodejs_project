<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>view_registration</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</head>

<body>
    <%- include ('./header.ejs') %>

        <div class="container-fluid index">
            <div class="h1">view food item</div>

            <div class="row">
                <div class="col-md-4">
                    <%- include ('./left.ejs') %>
                </div>
                <div class="col">

                    <% if(foodData.length>0){ %>
                        <table class="table table-bordered  border-black ">
                            <thead>
                                <tr>
                                    <th>Product Name</th>
                                    <th>Item Picture</th>
                                    <th>Update</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% for(let i=0; i<foodData.length; i++){%>
                                    <tr>
                                        <td><%= foodData[i].name %></td>
                                        <td><img src="<%= foodData[i].photo%>" alt="" height="100px"></td>
                                        <td >
                                           
                                            <a class="edit" href="/edit_5/<%= foodData[i]._id %>" >Edit<ta>
                                            <a class="delete" href="/delete_5/<%= foodData[i]._id %>" >Delete</a>
                                            
                                        </td>
                                    </tr>

                                <% }%>
                            </tbody>
                        </table>   
                        <% } else { %>
                        
                            no record found
                        <%} %>

                </div>
            </div>
        </div>

</body>

</html> 

